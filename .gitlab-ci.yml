image: node:22

stages:
  - deploy

pages:
  stage: deploy
  script:
    - npm ci
    - npm run verify
    - git diff --exit-code -- assets/search-index.js
    - npm run build
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
